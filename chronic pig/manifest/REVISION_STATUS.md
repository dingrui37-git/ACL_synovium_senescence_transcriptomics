# Revision status of the chronic-pig package

This repository was updated for the reviewer-response package on 2026-09-17.

- The dedicated Figure 5D early-versus-chronic summary was retired. Its figure files, source tables, script and dedicated logs are no longer part of the active package.
- Figure 5C was regenerated as the wide manuscript-layout export. The active Figure 5C files and script are located under the corresponding `figures_required`, `tables_required`, `scripts` and `optional_QC_audit` directories.
- The complete current revision package is `revision_submission_materials_20260911/`. Its `00_START_HERE` directory is the authoritative index for the reviewer-requested analyses, supplementary figures/data and reproducibility records.

The CSV files in this directory are retained as provenance records for the original chronic-pig minimal-package assembly. They describe the pre-revision copy operation and therefore may contain historical Figure 5D labels or source paths that are intentionally absent from the current working tree. They should not be used as the current file inventory; use the revision-package index and the repository tree instead.
